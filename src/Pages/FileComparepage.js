import React from "react";
import Layout from "../Components/Layout";
import FileCompare from '../Data/FileCompare';
const FileComparepage = () => {
  return (
    <Layout>
      <FileCompare/>
    </Layout>
  );
};

export default FileComparepage;
